diff --git a/target/asd_msg.c b/target/asd_msg.c
index 68a9d0d..cf623cd 100644
--- a/target/asd_msg.c
+++ b/target/asd_msg.c
@@ -1194,14 +1194,27 @@ STATUS process_jtag_message(struct asd_message* s_message)
                     explicit_bzero(line, sizeof(line));
                     for (int i = chain_bytes_length - 1; i >= 0; i--)
                     {
-                        snprintf(&line[pos], sizeof(line), "%02X",
-                                 chain_bytes[i]);
-                        pos = pos + 2;
-                        if (i % 2 == 0 && i != 0)
+                        size_t remaining_size = sizeof(line) - pos;
+
+                        if (remaining_size > 2)
                         {
-                            snprintf(&line[pos], sizeof(line), " ");
+                            snprintf(&line[pos], remaining_size, "%02X",
+                                     chain_bytes[i]);
+                            pos = pos + 2;
+                        }
+
+                        remaining_size = sizeof(line) - pos;
+
+                        if (i % 2 == 0 && i != 0 && remaining_size > 1)
+                        {
+                            snprintf(&line[pos], remaining_size, " ");
                             pos++;
                         }
+
+                        if (pos < sizeof(line))
+                        {
+                            line[pos] = '\0';
+                        }
                     }
 #ifdef ENABLE_DEBUG_LOGGING
                     ASD_log(ASD_LogLevel_Debug, ASD_LogStream_SDK,
@@ -2429,13 +2442,18 @@ STATUS asd_msg_read(void)
         }
         case READ_STATE_HEADER:
         {
-            void* buffer =
-                (void*)(&(msg->header) + msg_state.in_msg.read_index);
-            size_t num_to_read =
-                sizeof(msg->header) - msg_state.in_msg.read_index;
-
-            size_t size = asd_api_server_read(buffer, num_to_read, NULL);
+            size_t header_size = sizeof(msg->header);
+            void* buffer = NULL;
+            size_t num_to_read = 0;
+            size_t size = 0;
 
+            if (header_size > 0)
+            {
+                buffer = (void*)((char*)&(msg->header) + msg_state.in_msg.read_index);
+                num_to_read = sizeof(msg->header) - msg_state.in_msg.read_index;
+            }
+ 
+            size = asd_api_server_read(buffer, num_to_read, NULL);
             if (size > 0)
             {
                 result = ST_OK;
@@ -3383,4 +3401,4 @@ STATUS process_i2c_messages(struct asd_message* in_msg)
         free(builder);
     }
     return status;
-}
\ No newline at end of file
+}
diff --git a/target/target_handler.c b/target/target_handler.c
index 8c08521..1f2d5ec 100644
--- a/target/target_handler.c
+++ b/target/target_handler.c
@@ -1436,6 +1436,7 @@ STATUS target_write(Target_Control_Handle* state, const Pin pin,
             break;
         default:
 #ifdef ENABLE_DEBUG_LOGGING
+            gpio = state->gpios[ASD_PIN_TO_GPIO[pin]];
             ASD_log(ASD_LogLevel_Debug, stream, option,
                     "Pin write: unsupported pin '%s'", gpio.name);
 #endif
